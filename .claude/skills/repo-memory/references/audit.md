# Audit

For each memory in `REPO_MEM_DIR`, read its content and verify any file paths or factual claims still hold (read referenced files, run greps as needed). Report each memory as accurate, partially drifted, or stale, with specific suggestions for drifted or stale entries. End with a summary count.
