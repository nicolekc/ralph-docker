{
  "name": "Pluggable Modes and Multi-Session",
  "description": "Two capabilities: (1) Extract code-specific content from the base framework into a pluggable 'mode' layer, so the framework can serve non-coding domains (research, tax prep, etc.) by adding new modes without modifying the base. A PRD declares which mode it uses. (2) Make it easy to run multiple Ralph sessions against the same project concurrently. These are related: modes make the framework general-purpose, multi-session lets you run more of it.",
  "signoff": "architecture",
  "tasks": [
    {
      "id": "001",
      "description": "Generalize the base framework. seed.md, planner.md, ralph.md, and prd.md contain code-specific language (TDD, commits, diffs, test infrastructure, code review, 'tests pass'). The base framework should be domain-neutral — principles that apply whether you're writing code, preparing taxes, or conducting research. Code-specific content needs to move out so a mode layer can own it. Perspectives that are inherently code-specific (implementer, code-cleaner, code-reviewer, explorer) also need to move out of the base. Perspectives that are already general (architect, planner, design-reviewer, spec-reviewer, qa-engineer) stay in the base but may need light edits to remove code-flavored language. The planner's 'Common patterns' section currently lists code pipelines — those patterns should come from the active mode, not be hardcoded.",
      "outcome": "Base framework files read naturally for any domain. No code-specific terminology in the base layer. All code-mode content is preserved, just relocated into a separate layer.",
      "verification": "Read seed.md, planner.md, ralph.md, prd.md as if you're building a tax preparation pipeline. Nothing references code, tests, commits, or programming concepts. The base perspectives directory contains only domain-neutral roles.",
      "dependencies": [],
      "status": "pending",
      "pipeline": []
    },
    {
      "id": "002",
      "description": "Create the mode system and the 'code' mode. A mode adds domain-specific perspectives, principles (a seed addendum), and pipeline patterns to the base framework. The first mode is 'code' — it contains everything extracted from task 001 plus the code-specific perspectives. The mode system should be simple file layering: just a directory with the right files that get read when the mode is active. Not a plugin architecture, not a registry, not dynamic loading. Ralph reads the PRD's 'mode' field and loads the mode's files on top of the base. The planner sees base roles + mode roles when composing pipelines. Creating a new mode means adding a directory — no framework code changes. A PRD with no mode or a missing mode still works using only base roles.",
      "outcome": "A 'code' mode exists with all code-specific content. Ralph loads mode files based on the PRD's mode field. The planner composes pipelines from base + mode roles. The PRD template has a 'mode' field. Creating a new mode is just adding files — no other changes needed.",
      "verification": "An existing code PRD with mode 'code' produces identical behavior to today. A PRD with no mode works with only base roles. Someone could create a 'research' mode by following the code mode's pattern without reading any docs beyond the mode directory itself.",
      "dependencies": ["001"],
      "status": "pending",
      "pipeline": []
    },
    {
      "id": "003",
      "description": "Multi-session support. Currently, `ralph-start.sh` from a second terminal intercepts the existing Docker session (`docker start -ai` reattaches). And two `ralph-loop.sh` instances on the same worktree cause git conflicts. Users need 2+ concurrent Ralph sessions against the same project — e.g., parallel PRDs. The Docker terminal problem is trivially solved with `docker exec`. The git isolation problem needs worktrees or clones so each session has its own working tree. The parallel-agents research doc (ralph-context/designs/parallel-agents-research.md) has prior analysis — full clones recommended for Docker, worktrees for local. The solution should be simple enough to explain in one paragraph.",
      "outcome": "A user can launch a second Ralph session against the same project without disrupting the first. Each session has its own git working tree. The workflow is documented and easy.",
      "verification": "Start two sessions against the same project. Run different PRDs in each. Both complete without git conflicts or Docker session hijacking. The setup took under 2 minutes and no special knowledge.",
      "dependencies": [],
      "status": "pending",
      "pipeline": []
    }
  ]
}
